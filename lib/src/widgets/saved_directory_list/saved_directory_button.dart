import 'package:flutter/material.dart';

class SavedDirectoryButton extends StatelessWidget {
  const SavedDirectoryButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Hola'),
    );
  }
}
